mutation CreateShortAnswer($options: ProblemsArgs!) {
  createShortAnswer(options: $options) {
    id
    points
    contestId
    shortAnswerId
    shortAnswer {
      id
      question
      answer
      solution
    }
  }
}
