query FindContest($options: PaginationArgs!) {
  findContests(options: $options) {
    id
    name
    email
    website
    thumbnail
    description
    tags
    length
    startDate
    endDate
    private
    leaderboard
  }
}
